import styled from "styled-components";

const BannerContainer = styled.section`
    height: 100vh;
`

const Banner = ()=> {

    <BannerContainer>
    
    </BannerContainer>
}

export default Banner;